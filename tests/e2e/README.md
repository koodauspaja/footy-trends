# End-to-end tests

Place tests that exercise the application through its user-facing runtime here.
Keep these tests separate from unit and integration tests because they require
an application server and browser or equivalent runtime setup.

Run them with:

```bash
npm run test:e2e
```